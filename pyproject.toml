[build-system]
requires = ['setuptools']
build-backend = "setuptools.build_meta"

[project]
name = "sephirothos"
version = "0.1.0"
requires-python = ">=3.14,<3.15"

[tool.setuptools.packages.find]
where = ["src"]